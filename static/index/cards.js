const Game = {
  hasToTake
}
class Card {
  constructor(color, value, flip_direction, to_take) {
    this.color = color;
    this.value = value;
    this.flip_direction = flip_direction;
    this.to_take = to_take;
  }

  canPutOn(other) {
    if ()
  }
}  
